# Pinned dependencies for the scanner material-recognition system.
# Install into an isolated venv (NOT global Python) to avoid clashing with the
# lab PC's other tools (gradio/fastapi pin older numpy/starlette):
#
#   python -m venv scanner_system/.venv
#   scanner_system/.venv/Scripts/pip install -r scanner_system/requirements.txt
#
# streamlit is held below 1.50 so its starlette requirement stays compatible
# with the fastapi 0.115 used by the 3DAI API (starlette <0.46).

# GUI + data
streamlit<1.50
pymongo>=4,<5
pandas>=2,<3
pillow>=10,<12
# NOTE (2026-09-05 audit): pillow 11.x carries open advisories (PYSEC-2026-3453..3496)
# whose fix is pillow 12.3, but streamlit<1.50 declares pillow<12, and streamlit is
# held <1.50 for starlette compatibility with the 3DAI API's fastapi. The chain is
# fastapi/starlette -> streamlit<1.50 -> pillow<12. Lifting it means qualifying
# streamlit 1.53+ against the API first. Accepted for a non-internet-facing lab PC.
plotly>=5.18,<7          # interactive 3D height-map surface in the GUI

# imaging / reconstruction (host-side capture + fringe tools)
numpy>=1.26,<3
opencv-python-headless>=4.9
imageio>=2.34
scipy>=1.12
matplotlib>=3.8

# ML (material recognition / dataset work)
scikit-learn>=1.4

# hardware + http (ESP32 serial, scanner API client)
pyserial>=3.5
requests>=2.31

# spreadsheet manifests (pandas needs an engine to read/write .xlsx at all).
# Pure Python, no dependencies, so it cannot disturb the streamlit<1.50 pin.
openpyxl>=3.1
